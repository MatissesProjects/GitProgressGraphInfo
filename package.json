{
  "name": "gitprogressgraphinfo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/content.ts src/popup.ts --bundle --outdir=dist --minify --platform=browser && powershell -Command \"Copy-Item -Path 'src/manifest.json', 'src/styles.css', 'src/popup.html' -Destination 'dist' -Force\"",
    "watch": "esbuild src/content.ts src/popup.ts --bundle --outdir=dist --platform=browser --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3"
  }
}
